export default function ProductFinder({ className = null }) {
	return (
		<div
			className={`bg-white dark:bg-black dark:text-white flex justify-center items-center w-full min-h-lg ${className}`}>
			Product Finder
		</div>
	);
}
